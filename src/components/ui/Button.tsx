import { Pressable, TouchableOpacity, Text } from "react-native";


export const Button: React.FC = ({ }) => {
  return (
    <TouchableOpacity>
      <Text>Click me</Text>
    </TouchableOpacity>
  );
}
